# State, 12 September 2026

What exists, where it lives, and whether it survives.

## Durable: the branch `sound-function-research`

`becker929/anthonybecker.me`, branch `sound-function-research`. Squashed
history, no audio. Clone at `~/sandbox/sound-function` on the Mac.

- `analysis/` — feature extraction (`signal_features.py`, fixed 9 Sep),
  grid and downbeat finding, pump measure, stems analysis, classifier,
  figures. `analysis.run hits` must be followed by `analysis/hits_extra.py`
  or three columns go missing.
- `corpus2/` — the 508-track corpus: `tracks-2.csv` (744 fetched rows with
  licence and `download_url`), `results.json`, `stems_results.json`,
  `band_pump.json`, the analysis and text generators for parts three and
  four. No audio.
- `research/` — `hypotheses.md` (H1 to H47, 35 tested, table regenerated by
  `test_hypotheses.py`), `literature.md`, `literature-2.md`, `advice.md`,
  `knob-map.md`, `multitrack-first-results.md`, `measurement-fixes.md`,
  `listening-test-stimuli.md`, `live-experiments.md`, `live-multitrack-bounce.md`,
  `live-oneshot-pack.md`, `offline-plugins.md`, `mac-lab.md`, `channels.md`,
  `next.md`, `specs/`, `data/`.
- `results/live_rumble_bypass_v1/` — the Mac's bypass pack: two JSON
  results, two sidecars, MANIFEST. No wavs.
- `results/offline_plugins_v1/` — plugin parameter lists, Anthony's LFOTool
  state dump, and the flat pedalboard sweep kept as evidence.
- `results/live_multitrack_trim_v1/` — the 18 stems cut to content length:
  per-stem lengths, two bench reports (made before the pair-selector fix),
  MANIFEST.
- `lab/` — `runner.py` (`--local` mode reads `tracks/`, `refs/`,
  `stems/<song>/`), `duck_calibration.py`, `plugin_sweep.py`, `loop_hits.py`,
  `drive_fetch.py`, report renderers, embedding.
- `listen/` — `make_pairs.py` (old synthetic), `make_pairs_real.py`,
  `real_sweeps.py`, `realism_check.py`.
- `library/` — manifests and sources for the real one-shot library
  (`library/real/manifest.csv`, `sources.md`, per-file licence) and the
  synthetic ones. The 318 real wavs are NOT in git (gitignored `wav/`);
  they re-download from the archive.org items in `sources.md`.
- `synth/` — the test synthesiser and sweeps.
- `tools/` — `plainlint.py` with `vocab_simple.txt`, `glossary.txt`,
  `names.txt`; `sync_site_copy.py`.
- `ops/` — `notify.py`; `mac/` with the Reminders mirror, the mailbox
  poller, the launchd installer, `local.env.example`.
- `out/` — small derived tables: `library_real.csv`, `library_synth*.csv`,
  calibration JSON. Large outputs are gitignored.
- `requirements.txt`, `CLAUDE.md`, `docs/`.

## Durable: the site, `main` of the same repository

Live at https://anthonybecker.me, deployed by Cloudflare on push.

- Research: parts one to four at `/research/sound-function/part-{one..four}/`.
- Notes 1 to 11 at `/notes/<slug>/`, listed by `notes/manifest.json`:
  from-a-meter-to-an-engineer, one-producer, where-the-tracks-sit,
  four-horizons, the-session, the-split-that-is-not-there,
  the-knob-that-lied, the-stems-that-broke-the-ruler (with a 12 Sep
  postscript), three-wrong-rulers, the-instrument-problem (titled "The plan,
  with dates"), three-days-timed (12 Sep 18:11 UTC, the dated summary).
- Listening test at `/research/sound-function/listen/`: 31 pairs, set
  `v2-real`, built from CC BY / CC0 / public-domain one-shots with credits
  rendered; answers via `/api/listen` (POST) and `/api/listen/export` (GET,
  public). Three answers exist, all against the old synthetic set, from one
  producer of fifteen years on 8 Sep.
- The lab at `/lab`, gated by the `DEMO_PASSWORD` secret; the runner's token
  `LAB_TOKEN` was never set, so the runner cannot post to the site.
- A mirror of this repository at `/research/sound-function/repo/`, made by
  `tools/sync_site_copy.py`, which skips audio, stems, results dumps,
  `local.env`, logs, and (from 12 Sep) `docs/` and `CLAUDE.md`.
- The mailbox, pull request #25, draft, never merged.

## Durable: elsewhere

- Anthony's Drive folder `claude-research-2026-09-06` (id
  `1kyN0yo7LsarLa-4kPq5Ahv6JiGvtVWkd`): `tracks/` (3 of his tracks plus a
  zip), `refs/` (4 references), `multitracks/` (empty), `samples/`. The 502
  MB `live_multitrack_bounce_v1.zip` was shared from his other account.
- On the Mac: the Live rig (`~/.agents/skills/ableton-live-control/`,
  `~/sandbox/autodaw/hands/`), the 18 multitrack stems (untrimmed), the
  rumble bypass renders, the HW002 and SNTS clones under
  `~/_agent_scratch/`, and the HANDOFF.md log the previous Mac agent kept at
  `~/sandbox/autodaw/hands/sweeps/HANDOFF.md`.
- The ntfy topic (in `ops/local.env` on each machine; in the Mac agent's
  pasted prompt).

## Ephemeral: the web session's container

Dies with the container. Nothing here is needed.

- `corpus2/stems/`: 662 Demucs excerpts, drums and bass only after 11 Sep
  (vocals and other deleted for space), 3.6 GB. Regenerable from
  `tracks-2.csv` plus Demucs in about a day.
- `corpus2/wav/`, `corpus2/_raw/`: the downloaded corpus audio.
- `lab/drive/files/`: Anthony's downloaded tracks and references.
- The pre-squash git history with stems, tagged `archive/pre-clean-history`.
- Scratch logs and the extracted multitrack sidecars.

## Numbers as of today

| thing | value |
|---|---|
| corpus tracks measured | 508 (744 fetched rows) |
| separated excerpts | 650 usable of 662 |
| hypotheses | 47 registered, 35 tested |
| real one-shots measured | 318 |
| listening-test pairs live | 31 (set v2-real) |
| listening-test answers | 3 (set v1-synth) |
| knob map rows | 16: SNTS Kick (G) Decapitator Drive, StandardCLIP threshold; HW002 rumble LFOTool depth (offline); HW002 kick group bus, ten rows of which five are null (Roar bypassed) and three flat (compressor idle), Threshold live; three more at operating point 0.4, Ratio invertible |
| real multitrack stems | 18, from 2 sets; trimmed copies on the Mac (38% of the recorded audio was music) |
| pump measure | corpus estimator reads ~12 for a true 24 dB, ~14 for 40; division route within 0.5 dB up to 9 dB on real material |
| classifier, synthetic to real | 0.346 (chosen features), unchanged by the decay fix |
